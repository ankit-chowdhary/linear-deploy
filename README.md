# linear-deploy

Public deployment configurations for the Linear clone.
This repo contains NO source code — only Docker compose, Caddy config,
and shell scripts that the production server reads.

The actual application source lives in a separate private repo.
